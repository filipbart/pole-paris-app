import 'package:flutter/material.dart';

class PendingCarnetsScreen extends StatelessWidget {
  const PendingCarnetsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
